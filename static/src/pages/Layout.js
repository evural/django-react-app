import React from "react";
import Navbar from "../components/Navbar";

const Layout = () => {

    <div className="container layout">
		<Navbar logged_in={loggedIn} handle_signup={handle_signup} handle_login={handle_login} handle_logout={handle_logout} />
	</div>

}
